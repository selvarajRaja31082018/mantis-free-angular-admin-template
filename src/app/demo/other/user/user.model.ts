// user.model.ts
export interface User {
    id: number;
    name: string; // Adjust this based on your API response
    email: string;
    mobile: string; // Use the appropriate type (e.g., string)
  }
  